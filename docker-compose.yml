services:
  web:
    build:
      context: .
      args:
        BUILD_COMMIT_SHA: ${BUILD_COMMIT_SHA:-}
    container_name: otopro-web
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "3000:3000"
